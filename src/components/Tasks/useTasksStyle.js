import { makeStyles } from '@mui/styles';
import { createTheme } from '@mui/system';
const useTasksStyle = makeStyles(theme => ({
  completed: {
    backgroundColor: '#91f10bda',
  },
}));
export default useTasksStyle;
